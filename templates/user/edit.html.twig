{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <style>

    </style>

    <div class="container-md mt-5">
        {% if user %}
            <div class="component">
                <h1>{{ user.firstName }}</h1>
                <p>{{ user.email }}</p>
            </div>
        {% endif %}
    </div>
    <div class="container-md mt-5">
    {{ form(form) }}
    </div>
{% endblock %}